<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.virtualpain.munny">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.fruits.FruitActivity" />
        <activity android:name=".activities.counter.CounterActivity" />
        <activity android:name=".activities.countdown.CountdownActivity" />
        <activity android:name=".activities.anko.TryAnkoActivity" />
        <activity android:name=".activities.anko.AnkoListActivity" />
        <activity android:name=".activities.retrofit.TryRetrofitActivity"></activity>
    </application>

</manifest>